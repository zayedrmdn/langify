import 'package:flutter/material.dart';

class ForumTab extends StatelessWidget {
  const ForumTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Forums', style: TextStyle(fontSize: 24)),
    );
  }
}
