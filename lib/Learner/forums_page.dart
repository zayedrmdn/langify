import 'package:flutter/material.dart';

class ForumsPage extends StatelessWidget {
  const ForumsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Forums/Chats', style: TextStyle(fontSize: 24)),
    );
  }
}
